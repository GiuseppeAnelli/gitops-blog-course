apiVersion: v1
kind: Ingress
metadata:
  name: blog
  namespace: GiuseppeAnelli-blog
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: GiuseppeAnelli.blog.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blog
            port:
              number: 80